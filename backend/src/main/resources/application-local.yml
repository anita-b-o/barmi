spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5434}/${DB_NAME:barmi}
    username: ${DB_USER:barmi}
    password: ${DB_PASSWORD:barmi123}

  flyway:
    enabled: true
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5434}/${DB_NAME:barmi}
    user: ${DB_USER:barmi}
    password: ${DB_PASSWORD:barmi123}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

server:
  port: 8080

app:
  security:
    jwtSecret: ${JWT_SECRET:JWT_SIGNING_KEY=7f9c2ba4e88f827d616045507605853ed73b8094b9b2e0c3c5c7e5c7c4e1c9a1}
    jwtTtlMinutes: 60
    refreshTtlDays: 30
    allowDevIdentityHeader: true
  tenant:
    baseDomain: ${STORE_BASE_DOMAIN:example.com}
  notifications:
    email:
      mode: ${NOTIFICATION_EMAIL_MODE:logging}
      from: ${NOTIFICATION_EMAIL_FROM:}
  mercadoPago:
    webhookSecret: ${MP_WEBHOOK_SECRET:change-me}

management:
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  health:
    redis:
      enabled: false
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
