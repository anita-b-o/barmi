package com.barmi.app.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // place for shared beans (clock, object mapper configs, etc.)
}
